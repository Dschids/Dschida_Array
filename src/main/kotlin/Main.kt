fun main(args: Array<String>) {
    // create an array of at least 5 items, use a for loop to print them
    var ammo = arrayOf("Pistol", "Rifle", "Shotgun", "Sniper Rifle", "Energy Cells")
    println("Ammo Types:")
    for (item in ammo) {
        println(item)
    }
}